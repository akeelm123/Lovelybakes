import "server-only";
import Stripe from "stripe";
export function paymentsEnabled(){const key=process.env.STRIPE_SECRET_KEY;return process.env.PAYMENTS_ENABLED==="true"&&Boolean(key)&&Boolean(process.env.STRIPE_WEBHOOK_SECRET)&&(process.env.UAT_MODE!=="true"||key!.startsWith("sk_test_"));}
export function stripeClient():Stripe{const key=process.env.STRIPE_SECRET_KEY;if(!key)throw new Error("STRIPE_NOT_CONFIGURED");if(process.env.UAT_MODE==="true"&&!key.startsWith("sk_test_"))throw new Error("LIVE_KEY_BLOCKED_IN_UAT");return new Stripe(key)}
export async function createStripeCheckout(input:{orderId:string;email:string;items:Array<{name:string;quantity:number;unitPriceCents:number}>;deliveryFeeCents:number}){const baseUrl=process.env.PUBLIC_APP_URL;if(!baseUrl)throw new Error("PUBLIC_APP_URL_NOT_CONFIGURED");const line_items:Stripe.Checkout.SessionCreateParams.LineItem[]=input.items.map(item=>({quantity:item.quantity,price_data:{currency:"sgd",unit_amount:item.unitPriceCents,product_data:{name:item.name}}}));if(input.deliveryFeeCents>0)line_items.push({quantity:1,price_data:{currency:"sgd",unit_amount:input.deliveryFeeCents,product_data:{name:"Delivery"}}});return stripeClient().checkout.sessions.create({mode:"payment",payment_method_types:["card"],client_reference_id:input.orderId,customer_email:input.email,metadata:{orderId:input.orderId},success_url:`${baseUrl}/order/confirmation?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${baseUrl}/?checkout=cancelled`,line_items},{idempotencyKey:`checkout-${input.orderId}`})}
export function verifiedStripeEvent(payload:string|Buffer,signature:string){const secret=process.env.STRIPE_WEBHOOK_SECRET;if(!secret)throw new Error("STRIPE_WEBHOOK_NOT_CONFIGURED");return stripeClient().webhooks.constructEvent(payload,signature,secret)}
export function payNowAvailability(){return{configured:false,code:"PAYNOW_PROVIDER_NOT_SELECTED",message:"PayNow remains disabled until the CPO approves a provider and its operating model."}}
