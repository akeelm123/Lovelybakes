"use client";import{useState}from"react";import type{ManagedNotification}from"@/server/notifications";
const names:Record<string,string>={order_confirmed:"Order confirmed",order_preparing:"Preparing",order_ready:"Ready",order_fulfilled:"Fulfilled",order_cancelled:"Cancelled"};
export function NotificationManager({initial,configured}:{initial:ManagedNotification[];configured:boolean}){const[items,setItems]=useState(initial),[busy,setBusy]=useState<string>(),[error,setError]=useState("");async function retry(id:string){setBusy(id);setError("");try{const response=await fetch("/api/admin/notifications",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id})});const body=await response.json();if(!response.ok)throw new Error(body.error?.message??"Delivery failed");const refreshed=await fetch("/api/admin/notifications",{cache:"no-store"});setItems((await refreshed.json()).notifications)}catch(cause){setError(cause instanceof Error?cause.message:"Delivery failed")}finally{setBusy(undefined)}}return <main className="admin-main"><div className="admin-heading"><div><h1>Customer emails</h1><p>Track order messages and retry failed delivery.</p></div></div><div className="admin-preview-note">{configured?"Email delivery is connected.":"Delivery is safely paused until an email provider and verified sender are connected."}</div>{error&&<div className="admin-message admin-error" role="alert">{error}</div>}<div className="order-list">{items.map(item=><article className="admin-panel notification-card" key={item.id}><div><strong>{names[item.templateKey]}</strong><p>{item.recipientEmail}</p><small>Order {item.orderId.slice(0,8).toUpperCase()} · {new Date(item.createdAtUtc).toLocaleString("en-SG")}</small></div><div><span className={`delivery-status delivery-${item.deliveryStatus}`}>{item.deliveryStatus}</span><small>{item.attemptCount} attempt{item.attemptCount===1?"":"s"}</small>{item.deliveryStatus!=="sent"&&<button className="primary-button" disabled={!configured||busy===item.id} onClick={()=>void retry(item.id)}>Retry</button>}</div></article>)}{items.length===0&&<div className="admin-panel"><p>No customer emails have been queued.</p></div>}</div></main>}
