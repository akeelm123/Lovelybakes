import { redirect } from "next/navigation";import { currentAdmin } from "@/server/admin-session";import { listOrders } from "@/server/orders";import { AdminHeader } from "@/components/admin-header";import { OrderManager } from "@/components/order-manager";export const dynamic="force-dynamic";export default async function OrdersPage(){if(!await currentAdmin())redirect("/admin/login");return <><AdminHeader/><OrderManager initial={await listOrders()}/></>}
