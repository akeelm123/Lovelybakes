import {redirect} from "next/navigation";import {currentAdmin} from "@/server/admin-session";import {AdminHeader} from "@/components/admin-header";import {listNotifications,emailConfigured} from "@/server/notifications";import {NotificationManager} from "@/components/notification-manager";export const dynamic="force-dynamic";export default async function NotificationsPage(){if(!await currentAdmin())redirect("/admin/login");return <><AdminHeader/><NotificationManager initial={await listNotifications()} configured={emailConfigured()}/></>}
