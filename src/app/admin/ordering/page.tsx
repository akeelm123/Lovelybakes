import { redirect } from "next/navigation";import { currentAdmin } from "@/server/admin-session";import { getOrderingRule } from "@/server/ordering";import { OrderingEditor } from "@/components/ordering-editor";import { AdminHeader } from "@/components/admin-header";export const dynamic="force-dynamic";export default async function OrderingPage(){if(!await currentAdmin())redirect("/admin/login");return <><AdminHeader/><OrderingEditor initial={await getOrderingRule()}/></>}
