import { adminOrderUpdateSchema, orderStatusSchema } from "@/domain/order";
import { requireAdmin, AuthorizationError } from "@/server/auth";
import { listOrders,updateOrderStatus,createSyntheticOrder } from "@/server/orders";
import { problem,safeJson } from "@/server/http";
function failure(error:unknown){if(error instanceof AuthorizationError)return problem(error.status,"AUTHORIZATION_FAILED",error.message);if(error instanceof Error&&error.message==="INVALID_ORDER_TRANSITION")return problem(409,"INVALID_ORDER_TRANSITION","Complete each fulfilment step in order.");if(error instanceof Error&&error.message==="EDIT_CONFLICT")return problem(409,"EDIT_CONFLICT","This order changed in another window. Reload before updating.");return problem(503,"SERVICE_UNAVAILABLE","Order service is temporarily unavailable.")}
export async function GET(request:Request){try{await requireAdmin(request);const value=new URL(request.url).searchParams.get("status"),status=value===null?undefined:orderStatusSchema.safeParse(value);if(status&&!status.success)return problem(400,"INVALID_STATUS","Unknown order status.");return Response.json({orders:await listOrders(status?.data)},{headers:{"Cache-Control":"no-store"}})}catch(error){return failure(error)}}
export async function POST(request:Request){try{const actor=await requireAdmin(request);if(!actor.sub)return problem(403,"MISSING_SUBJECT","Identified administrator required.");const parsed=await safeJson(request);if(!parsed||typeof parsed!=="object"||Object.keys(parsed).length!==1||!("createSynthetic" in parsed)||(parsed as {createSynthetic:unknown}).createSynthetic!==true)return problem(400,"INVALID_REQUEST","Only synthetic UAT orders can be created here.");return Response.json({orderId:await createSyntheticOrder(actor.sub)},{status:201})}catch(error){return failure(error)}}
export async function PATCH(request:Request){try{const actor=await requireAdmin(request);if(!actor.sub)return problem(403,"MISSING_SUBJECT","Identified administrator required.");const input=adminOrderUpdateSchema.safeParse(await safeJson(request));if(!input.success)return problem(400,"INVALID_UPDATE","A valid order, version and status are required.");const order=await updateOrderStatus(input.data.orderId,input.data.status,actor.sub,input.data.version);return order?Response.json({order},{headers:{"Cache-Control":"no-store"}}):problem(404,"ORDER_NOT_FOUND","Order not found.")}catch(error){return failure(error)}}
