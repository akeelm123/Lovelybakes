import type { MetadataRoute } from "next";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.PUBLIC_APP_URL??"https://lovelybakes.vercel.app";return["","/privacy","/terms"].map((path)=>({url:base+path,lastModified:new Date("2026-09-05"),changeFrequency:path?"yearly":"weekly",priority:path?0.3:1}))}
